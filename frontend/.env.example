# Base URL of the backend API.
# Leave empty when using the Vite dev proxy (frontend + backend on localhost).
# For a deployed backend, set e.g. VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
